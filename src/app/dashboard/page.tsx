import {currentUser} from "@/lib/auth";import {getOperationalStats} from "@/lib/stats";import Charts from "@/components/dashboard/Charts";import {flights} from "@/lib/flights";import {Activity,Armchair,CircleDollarSign,Plane,TicketCheck,UsersRound} from "lucide-react";
export const metadata={title:"Operations dashboard"};
export default async function Dashboard(){const [user,stats]=await Promise.all([currentUser(),getOperationalStats()]);const cards=[[Plane,"Published flights",stats.totalFlights,"Live inventory"],[Activity,"Active flights",stats.activeFlights,"Approved routes"],[TicketCheck,"Total bookings",stats.totalBookings,"MongoDB records"],[CircleDollarSign,"Paid revenue",`$${stats.totalRevenue.toLocaleString()}`,"Settled payments"],[UsersRound,"Passengers",stats.totalPassengers,"Confirmed seats"],[Armchair,"Available seats",stats.availableSeats,"Current capacity"]] as const;return <div className="bg-slate-50 py-10"><div className="container"><div className="mb-8"><span className="eyebrow">OPERATIONS CENTER</span><h1 className="section-title">Welcome back, {user?.name}</h1><p className="muted">A database-backed view of schedules, bookings and performance.</p></div><div className="mb-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-6">{cards.map(([Icon,label,value,note])=><div className="card p-4 lg:col-span-2" key={label}><div className="flex justify-between"><span className="grid size-10 place-items-center rounded-xl bg-blue-50 text-blue-600"><Icon size={19}/></span><small className="text-slate-400">{note}</small></div><div className="mt-5 text-2xl font-black">{value}</div><div className="text-sm text-slate-500">{label}</div></div>)}</div><Charts monthlyRevenue={stats.monthlyRevenue} destinationData={stats.destinationData}/><div className="card mt-5 overflow-hidden p-5"><h3 className="mb-1 font-black">Published route activity</h3><p className="mb-4 text-xs text-slate-500">Stable schedule records currently available to travellers.</p><div className="overflow-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr>{["Flight","Route","Departure","Class","Status"].map(x=><th className="p-3" key={x}>{x}</th>)}</tr></thead><tbody>{flights.slice(0,5).map(f=><tr className="border-t" key={f.id}><td className="p-3 font-bold">{f.flightNumber}</td><td className="p-3">{f.departureAirport} → {f.destinationAirport}</td><td className="p-3">{f.departureDate}, {f.departureTime}</td><td className="p-3">{f.travelClass}</td><td className="p-3"><span className="rounded-full bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700">{f.status}</span></td></tr>)}</tbody></table></div></div></div></div>}
